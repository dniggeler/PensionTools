<MudCard Class="pa-4">
    <EditForm Model="@Person" OnValidSubmit="HandleValidSubmit">
        <DataAnnotationsValidator />
        <ValidationSummary />

        <PersonDetails IsReadOnly="false" Person="@Person"/>
        
        <MudCardActions>
            <MudIconButton Icon="@Icons.Material.Filled.Done" ButtonType="ButtonType.Submit" />
        </MudCardActions>

    </EditForm>
</MudCard>


@code
{
    [Parameter]
    public PersonViewModel Person { get; set; }

    [CascadingParameter] MudDialog MudDialog { get; set; }

    private async Task HandleValidSubmit()
    {
        await MudDialog.CloseAsync(DialogResult.Ok(Person));
    }
}
